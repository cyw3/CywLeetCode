package com.cyw.algorithms.divideandconquer;

/**
 * 使用分治方法，求解 最大子数组问题。即求一包含负值的数组的子数组，使该子数组和最大
 * @author cyw
 *
 */
public class MaximunSubArray {

	/**
	 * A[low..high]
	 * 分为三种情况：
	 * 完全位于子数组A[low..mid]中
	 * 完全位于子数组A[mid+1..high]中
	 * 跨越了中点mid
	 * 
	 * 所以选出这三种情况下，和最大的
	 */
//	public static 
}
